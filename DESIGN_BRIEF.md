# 专注树 FocusTree · 设计简报（功能导向）

> 目的：给设计师一份**只讲功能与目标、不预设视觉方案**的输入。视觉表达完全开放，请自由发挥，不要被任何既有实现束缚。

---

## 1. 一句话

专注树是给**自由职业者 / 内容创作者**用的「**外脑 + 个人助理 + 成长地图**」：帮他们把脑子里散落的项目和任务，梳理成一张能持续生长、能看清优先级的地图，并有一个 AI 助理陪着拆解、排序、复盘。

产品的核心隐喻是「**一棵成长的树**」——用户的事业像树一样开枝散叶。这个隐喻是产品身份的一部分，但**如何用视觉表达这棵树，完全交给设计**。

---

## 2. 用户与场景

- **谁**：一个人干活的创作者/自由职业者，手上同时有好几摊事（做内容、接客户、个人成长…），容易乱、容易焦虑、容易忘。
- **他要什么**：一眼看清「我现在有哪些方向、每个方向进展到哪、接下来最该做什么」；以及一个能对话、会记住他、帮他想清楚的助理。
- **情绪基调**：这是一个高频、长期陪伴的个人工具，用户希望它**让人安心、愿意每天回来看**，而不是又一个让人有压力的待办清单。

---

## 3. 信息模型（要呈现的数据）

一棵层级结构：

```
方向/项目 (project)
   └── 分类 (category)
          └── 任务 (task)
```

每个节点带这些**属性/状态**，都是需要在界面上以某种方式被感知的：

- **状态**：进行中 / 已完成 / 休眠搁置
- **优先级**：低 / 普通 / 高 / 紧急
- **截止日期**：可能逾期、今天到期、临近
- **权重 / 分量**：这个方向或任务占用户多少精力（有的项目是主力，有的是副业）
- **名称、简介、AI 备注**等文本

---

## 4. 界面构成（有哪些"面"要设计）

### 主界面（两个区域）
1. **可视化区（主角）**：把上面的层级数据呈现成产品核心画面。用户在这里浏览全局、点选节点、增删改、调整结构。
2. **AI 对话区**：和助理聊天的地方，可收起给可视化让空间。

### AI 对话区里的内容
- 当前阶段目标的展示与设置
- 对话气泡（用户 / 助理）
- 助理的**结构化建议卡片**：比如「我建议这样拆解，要不要一键应用到你的地图上」、「我建议各方向这样分配精力」、「为什么我这么推荐（推理过程可展开）」
- 每条回复的来源/成本等元信息（次要）

### 辅助面板 / 弹层
- 节点详情与编辑（改状态、优先级、日期等）
- 新建节点
- 历史对话、AI 对你的记忆、AI 推荐记录（含命中率）、周回顾
- 数据备份与恢复
- 顶部工具栏（视图控制、撤销重做、开关对话等）
- 悬浮提示、右键菜单

---

## 5. 关键交互（设计需要支持的动作）

- 在可视化里**增删节点、移动/重排、改层级、折叠展开、重命名**
- 点选节点看详情、改属性
- 缩放与平移浏览全局
- 与 AI 对话，并把 AI 的建议**一键落到可视化上**
- 在对话里提到某个任务时，能和可视化里的节点**互相呼应/高亮**

---

## 6. 设计要解决的核心问题（写成目标，解法开放）

这些是设计的真正挑战，怎么解**完全开放**：

1. **可视化怎么既好看又好用**：数据是动态的——节点数量、层级深度因人而异、随时在变。要在**任意数据规模**下都成立：既有整体的美感与产品气质，又能让用户看清结构、读清标签、快速操作。这是整个设计的成败关键。

2. **怎么一眼传达"轻重缓急"**：让用户扫一眼就能感知——哪个方向是主力/走得深、哪个搁置了、哪个任务紧急或逾期、哪里最需要立刻处理。用什么视觉语言（形态、色彩、大小、动效…）来编码这些，开放。

3. **怎么让它"活"起来、有成长感与情感陪伴**：这是"成长地图"不是"待办列表"。完成、推进、里程碑，能不能带来正向的情感反馈？开放。

4. **信息密度的平衡**：对话区信息很密（对话 + 多种建议卡 + 元信息），要有层级不压迫；可视化区要留白呼吸。

5. **有机/优雅的画面与功能性控件如何共存**：既要有设计感，又不能牺牲可操作性（点选、拖拽、编辑的清晰度）。

---

## 7. 约束（真实的、非视觉的）

- **平台**：Web 应用（桌面为主）。
- **动态数据驱动**：一切随用户数据实时变化、可交互、可动画——不是静态海报（这一点会影响可视化的技术选择，但不预设怎么画）。
- **性能**：可视化在节点较多时仍要流畅。

---

## 8. 品牌调性（轻量参考，可推翻）

用户的大方向偏好是「**温润、自然、安静、有质感、不喧闹**」，希望**精致、有设计感、不像通用模板**。但这只是气质参考——**配色、形态、风格请自由探索**，不必沿用任何现有版本；如果你有更好的方向，欢迎完全重新定义。

> 补充：这是一个**视觉门槛很高**的项目，最重要的是那块"树/成长地图"的可视化——它值得一个真正独特、令人眼前一亮的设计，而不是安全的默认。
