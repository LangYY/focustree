# 工作进度 — 2026-05-12

## 背景

6 项交互增强需求（详见 `.claude/plans/flickering-dancing-tiger.md`），当前处于调试收尾阶段。

## 完成状态

| # | 功能 | 状态 |
|---|------|------|
| F1 | 分支末端加号按钮 | 已完成 |
| F2 | 手势/鼠标操作（pinch=缩放，swipe=平移，click=选中，dblclick=折叠） | 已完成 |
| F3 | 拖动分支到另一项目（合并/移动） | **仍有 BUG** |
| F4 | 对话输入框增高+可拖拽 | 已完成 |
| F5 | 分支权重编辑+AI协同 | 已完成 |
| F6 | 停止按钮+消息队列 | 已完成 |

## F3 当前状态

### 已实现的功能
- TreeView.jsx：SVG 层原生事件代理方式实现拖拽（非 d3.drag，避免与 D3 click/dblclick 冲突）
- useTree.js：`moveNode()` 和 `mergeNode()` 方法，含环形引用检测和 undo 支持
- App.jsx：`handleDropBranch` 弹窗选择「移动/合并/取消」

### 今天做的修复（第三次会话）
1. **TodayCard 遮挡鼠标事件** — TodayCard 使用 `absolute top-3 left-3 right-3 z-10` 覆盖整行宽度，拦截了 SVG 上的鼠标事件
   - 修复：外层 `pointer-events-none`，内部交互元素 `pointer-events-auto`
   - 文件：`src/components/Tree/TodayCard.jsx`

2. **Zoom filter 缺少 pointerdown** — D3 v7 可能在触控板环境使用 Pointer Events，filter 只拦截了 mousedown
   - 修复：filter 条件增加 `event.type === 'pointerdown'`
   - 文件：`src/components/Tree/TreeView.jsx` 第 256 行附近

3. **三指拖拽触发浏览器文本选中** — macOS 三指拖拽触发浏览器选区，消费 mousemove 事件
   - 修复：mousedown 时立即 `event.preventDefault()` + SVG 加 `userSelect: none` + `touchAction: none`
   - 文件：`src/components/Tree/TreeView.jsx`

### 拖拽实现架构
```
TreeView.jsx 中独立 useEffect([]) 
  → svgEl.addEventListener('mousedown', onMouseDown)  // 原生事件，非 D3
  → 事件代理：event.target.closest('.node') 找到节点容器
  → 仅 project 类型节点可拖拽
  → 4px 阈值确认拖拽意图
  → mouseup 时 elementsFromPoint() 检测目标项目节点
  → 调用 onDropRef.current(sourceNode, targetNode)
```

### 可能还存在的问题
- 三指拖拽的 `preventDefault()` 修复未经过实际验证
- 如果 TodayCard 在生成聚焦内容后变得更高，可能仍会遮挡部分节点
- 拖拽时的视觉反馈较弱（仅透明度降到 0.4）

## 调试方式
- 开发服务器：`http://localhost:5173`（Vite HMR）
- 构建验证：`npm run build`

## 关键文件
- `src/components/Tree/TreeView.jsx` — 拖拽核心逻辑、手势操作
- `src/components/Tree/TodayCard.jsx` — 今日聚焦卡（曾遮挡 SVG）
- `src/App.jsx` — handleDropBranch、各种 handler
- `src/hooks/useTree.js` — moveNode、mergeNode
