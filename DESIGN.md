# DESIGN — 专注树界面与交互规范

> 版本：1.0 · 建立于 2026-08-08
>
> 本文是 UI 重做的唯一执行依据。所有数值都是规范值，不是建议值；偏离需要先改本文。
> 产品逻辑以 `PRODUCT_SPEC.md` 为准，本文只规定「长什么样、怎么操作」。
> 工程原则以 `AGENTS.md` 为准，本文不覆盖它。

---

## 0. 这次要解决的问题

当前界面的具体缺陷，逐条对应到本文的解决方案：

| 现状问题 | 解决方案位置 |
| --- | --- |
| 默认 Tailwind 灰蓝配色，没有设计 token，没有识别度 | §2 设计令牌 |
| 右侧最多同时堆三块面板（节点详情 + 审计 + 对话），树被挤成一条缝 | §3 布局架构 / §5 右抽屉 |
| 顶栏 12 个同等份量的文字按钮，无主次 | §4 顶栏 / §5 左导轨 / §6 画布控件簇 |
| `directPriority` / `branchPriority` / `cultivationScore` 只被编码成「枝干粗细」一个通道，其余埋在调试面板 | §7 树画布可视化 |
| 「今天该做什么」由一张可随手关掉的浮动卡片承担 | §8.3 今日聚焦 |
| AI 的草案与优先级提案只活在聊天流里，滚上去就找不回 | §8.2 待确认收件箱 |
| 阶段目标（全局上下文）埋在对话面板顶部 | §4 顶栏目标 chip |
| 大量语义只靠颜色编码，色盲与低对比环境下不可读 | §10 可访问性 |

---

## 1. 设计原则

产品原则见 `PRODUCT_SPEC.md` §7。以下是从中推导出的**界面**原则，冲突时以本节为准：

1. **树是画布，不是组件。** 树占据主区域的全部空间，永远存在。所有其他界面都是覆盖在它之上或紧贴边缘的层，不允许有第二个东西和树平分主区域。
2. **三个分数是三个独立视觉通道。** `directPriority`、`branchPriority`、`cultivationScore` 分别映射到互不干扰的物理属性，绝不合并成一个条或一个数字。分别对应现在 / 未来 / 过去。
3. **颜色表示归属，不表示分数。** 色相唯一的含义是「属于哪条主线」。分数用尺寸、粗细、辉光、环纹表达。
4. **任何需要用户确认的东西都必须有常驻位置。** 提案不能只存在于会滚走的聊天流里。
5. **每个语义至少两个通道。** 依赖颜色的编码必须同时有形状、位置或文字冗余。
6. **生长是本产品的动效语言。** 出现用「长出来」，消失用「收回去」，不用滑入滑出和淡入淡出。
7. **克制信息密度，但不隐藏证据。** 默认视图安静；用户主动追问时（hover、选中、展开审计）必须能拿到完整的计算依据。

---

## 2. 设计令牌

### 2.1 落地方式

新建 `src/styles/tokens.css`，由 `src/index.css` 第一行 `@import`。使用 Tailwind v4 的 `@theme`（项目已是 `tailwindcss@4` + `@tailwindcss/vite`，CSS-first 配置，**不要**创建 `tailwind.config.js`）。

```css
/* src/styles/tokens.css */
@theme {
  /* 语义色引用 CSS 变量，主题切换只改变量 */
  --color-canvas:        var(--ft-canvas);
  --color-surface:       var(--ft-surface);
  /* …见下表… */
}

:root, [data-theme="dark"] { /* 深色变量 */ }
[data-theme="light"]       { /* 浅色变量 */ }
```

主题切换写 `document.documentElement.dataset.theme`，持久化到 `localStorage.ft_theme`，取值 `dark` | `light` | `system`，**默认 `dark`**。`system` 时监听 `matchMedia('(prefers-color-scheme: light)')`。

**硬性要求：重做完成后，`src/` 下不允许出现任何硬编码颜色字面量**（`#0f1117`、`gray-900`、`blue-600`、`STATUS_COLOR` 等全部清除）。唯一例外是 `tokens.css` 自身，以及 §2.3 分支色板的定义文件。

### 2.2 语义色

主题代号：深色 **「林夜」**，浅色 **「晨纸」**。

| 令牌 | 用途 | 林夜（默认） | 晨纸 |
| --- | --- | --- | --- |
| `--ft-canvas` | 树画布背景（最深/最纸） | `#0B0F0C` | `#F3F0E7` |
| `--ft-surface` | 应用底、顶栏、左导轨 | `#111611` | `#FAF8F2` |
| `--ft-surface-raised` | 抽屉、卡片、浮层 | `#171E17` | `#FFFFFF` |
| `--ft-surface-sunken` | 输入框、代码块、进度槽 | `#0D120D` | `#EDEAE0` |
| `--ft-surface-hover` | 可点击项 hover 底 | `#1F281E` | `#F0EDE3` |
| `--ft-overlay-scrim` | 模态遮罩 | `rgba(8,12,9,.66)` | `rgba(40,44,38,.34)` |
| `--ft-border-subtle` | 分组内分隔 | `#1E271E` | `#E6E2D6` |
| `--ft-border` | 常规描边 | `#2B362A` | `#D8D3C4` |
| `--ft-border-strong` | 强调描边、focus 前置 | `#3E4B3C` | `#B9B3A2` |
| `--ft-text-primary` | 正文、标题 | `#E4E8DC` | `#232821` |
| `--ft-text-secondary` | 次级说明 | `#A3AD9C` | `#5A6356` |
| `--ft-text-tertiary` | 元信息、占位 | `#6E7A6B` | `#8B9385` |
| `--ft-text-inverse` | 强调色底上的文字 | `#0B0F0C` | `#FBFAF5` |

**功能色**（同时给出 base / hover / soft 三档；soft 是半透明底色，用于选中态与标签底）：

| 令牌 | 含义 | 林夜 | 晨纸 |
| --- | --- | --- | --- |
| `--ft-accent` | 嫩芽绿，主动作、选中、确认 | `#7FBF6A` | `#3F8C4C` |
| `--ft-accent-hover` | | `#93CE7F` | `#357743` |
| `--ft-accent-soft` | | `rgba(127,191,106,.14)` | `rgba(63,140,76,.12)` |
| `--ft-warn` | 赭石，期限临近、需注意 | `#D89A57` | `#9A6520` |
| `--ft-warn-soft` | | `rgba(216,154,87,.14)` | `rgba(154,101,32,.12)` |
| `--ft-danger` | 莓红，逾期、破坏性操作 | `#D46A60` | `#A93F36` |
| `--ft-danger-soft` | | `rgba(212,106,96,.14)` | `rgba(169,63,54,.12)` |
| `--ft-ai` | 麻色，AI 归属（消息、提案、思考） | `#C9B99A` | `#8A7550` |
| `--ft-ai-soft` | | `rgba(201,185,154,.12)` | `rgba(138,117,80,.10)` |

AI 用「麻/亚麻」而不是紫或蓝，理由有两条：一是与 §2.3 分支色板全部拉开距离，AI 的痕迹永远不会被误认成某个项目；二是隐喻上 AI 是「纸上的批注」，不是另一条主线。

### 2.3 分支色板

色相唯一的含义是「属于哪条顶层主线」。定义在 `src/lib/branchPalette.js`，9 色循环，取自自然材质而非 Tailwind 饱和色：

| # | 名 | 林夜 | 晨纸 |
| --- | --- | --- | --- |
| 1 | 苔绿 | `#6E9E5A` | `#4E7A3E` |
| 2 | 赤陶 | `#C97B54` | `#A0562F` |
| 3 | 靛青 | `#5D82B8` | `#3D5F91` |
| 4 | 芥黄 | `#C2A24C` | `#8E7223` |
| 5 | 藤紫 | `#8F7FB0` | `#6A5A8C` |
| 6 | 锈红 | `#B85C57` | `#8F3B36` |
| 7 | 松石 | `#4E9E96` | `#2F7972` |
| 8 | 灰蓝 | `#7E93A6` | `#556B7E` |
| 9 | 樱褐 | `#B77E8E` | `#8C5566` |

保留现有 `shadeBranchColor(base, depth, status)` 的思路（层级越深越浅、越低饱和），但常数改为：

```
depthStep   = min(depth, 5)
saturation  = clamp(s0 * (0.96 - depthStep * 0.030), 0.34, 0.86)
lightness   = clamp(l0 + depthStep * 0.048, 0.40, 0.74)   // 晨纸主题改为 l0 - depthStep * 0.040，下限 0.26 上限 0.58
done    → saturation *= 0.38, lightness *= 0.92
dormant → saturation *= 0.26, lightness *= 0.84
```

用户在节点上显式设置的 `color` 优先于色板；`DEFAULT_PROJECT_COLOR` 视为「未设置」，走色板。这条现有逻辑保留。

### 2.4 字体

```
--ft-font-sans:  "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
                 "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei",
                 "Source Han Sans SC", "Noto Sans SC", sans-serif;
--ft-font-serif: "Source Han Serif SC", "Noto Serif SC", "Songti SC",
                 Georgia, "SimSun", serif;
--ft-font-mono:  "JetBrains Mono", "SF Mono", ui-monospace, "Cascadia Mono",
                 Consolas, monospace;
```

**不下载 web font，全部系统栈。**

- `--ft-font-serif` 只允许用在三处：空状态标题、周回顾正文、模式切换时的大标题。且只允许 ≥ 20px（中文衬线小字号在 Windows 上会退化成宋体，不可读）。
- `--ft-font-mono` 用于所有数字：分数、token 数、耗时、费用、日期偏移。理由是等宽让纵向对齐的数字可比较。

字号阶（含行高）：

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--ft-text-2xs` | 10px / 1.4 | 角标、图例、极密元信息 |
| `--ft-text-xs` | 11px / 1.5 | 元信息、标签 |
| `--ft-text-sm` | 12px / 1.6 | 控件文字、表单标签 |
| `--ft-text-base` | 13px / 1.7 | 界面正文 |
| `--ft-text-md` | 15px / 1.75 | **对话正文**（当前 14px 偏小，长文本吃力） |
| `--ft-text-lg` | 18px / 1.5 | 面板标题 |
| `--ft-text-xl` | 22px / 1.4 | 视图标题 |
| `--ft-text-2xl` | 28px / 1.3 | 空状态、周回顾开场（衬线） |

字重只用 400 / 500 / 600 三档。中文不使用 700 以上，会糊。

### 2.5 间距、圆角、投影

```
--ft-space-1: 4px    --ft-space-2: 8px    --ft-space-3: 12px
--ft-space-4: 16px   --ft-space-5: 20px   --ft-space-6: 24px
--ft-space-8: 32px   --ft-space-10: 40px  --ft-space-12: 48px

--ft-radius-xs: 4px    小徽章
--ft-radius-sm: 6px    输入框、小按钮
--ft-radius-md: 10px   按钮、tab
--ft-radius-lg: 14px   卡片、下拉
--ft-radius-xl: 20px   浮层、对话气泡
--ft-radius-full: 999px
```

投影带绿黑色调，不用纯黑（纯黑投影在暖色底上发脏）：

```
林夜：
--ft-shadow-sm: 0 1px 2px rgba(5,9,6,.45)
--ft-shadow-md: 0 4px 16px -4px rgba(5,9,6,.55)
--ft-shadow-lg: 0 16px 48px -12px rgba(5,9,6,.66)
晨纸：
--ft-shadow-sm: 0 1px 2px rgba(60,55,40,.08)
--ft-shadow-md: 0 4px 16px -4px rgba(60,55,40,.12)
--ft-shadow-lg: 0 16px 48px -12px rgba(60,55,40,.18)
```

### 2.6 动效

```
--ft-dur-1: 120ms   即时反馈（hover、按下、focus）
--ft-dur-2: 220ms   面板、tab、浮层
--ft-dur-3: 420ms   节点生长、节点消失
--ft-dur-4: 700ms   枝干延伸、树布局重排

--ft-ease-grow:   cubic-bezier(.22,.68,.28,1)   生长，末端缓慢定型
--ft-ease-settle: cubic-bezier(.32,.72,0,1)     面板落位
--ft-ease-out:    cubic-bezier(.16,1,.3,1)      通用退出
```

`@media (prefers-reduced-motion: reduce)`：所有 `--ft-dur-*` 覆盖为 `1ms`，保留 opacity 过渡，禁用所有呼吸/脉冲循环动画。

---

## 3. 布局架构

```
┌──────────────────────────────────────────────────────────────────────┐
│ 顶栏  48px   [枝形标记 专注树] ····· [阶段目标 chip] ····· [撤销/前进][账户] │
├────┬──────────────────────────────────────────────┬──────────────────┤
│ 左 │                                              │  右抽屉           │
│ 导 │              树 画 布（主角）                  │  380px 可拖       │
│ 轨 │                                              │  ┌──tab 堆──┐    │
│ 56 │   ┌今日条┐                       ┌控件簇┐      │  │对话 待确认 │    │
│ px │                                              │  │详情 审计   │    │
│    │   ┌图例┐                                      │  └──────────┘    │
├────┴──────────────────────────────────────────────┴──────────────────┤
```

- **顶栏**：全局身份与全局状态。高度 `--ft-topbar-h: 48px`，底边 `--ft-border-subtle`。
- **左导轨**：去哪里 + 打开什么。宽度 `--ft-rail-w: 56px`，右边 `--ft-border-subtle`。
- **主区域**：当前模式的内容，铺满。
- **右抽屉**：唯一的右侧区域。宽度 `--ft-drawer-w`，默认 380px，可拖拽 320–560px，持久化到 `localStorage.ft_drawer_w`。**任何时刻最多一个抽屉，不允许并排。**

组件文件：`src/components/Shell/AppShell.jsx`、`TopBar.jsx`、`LeftRail.jsx`、`Drawer.jsx`。`App.jsx` 只负责数据编排与状态，不再直接写布局样式。

### 3.1 窗口宽度响应

| 视口宽度 | 行为 |
| --- | --- |
| ≥ 1280px | 完整布局，抽屉推挤画布 |
| 1024–1279px | 抽屉改为**覆盖**画布（浮在上层，带 `--ft-shadow-lg`），画布不缩窄 |
| < 1024px | 左导轨收成 44px 纯图标；抽屉覆盖且宽度 = min(380, 100vw - 88px) |
| < 768px | 不做适配。移动端不在本次范围（见 §12 不做清单） |

---

## 4. 顶栏

从左到右三个区，中间区绝对居中：

**左区**
- 品牌标记：18×18 内联 SVG，一个三叉枝的极简线条（`--ft-accent` 描边 1.5，round cap）。禁止 emoji。
- 「专注树」`--ft-text-base` / 500 / `--ft-text-primary`。

**中区 —— 阶段目标 chip（本次新增的重要变化）**

阶段目标是全局上下文，影响所有排序，当前却埋在对话面板顶部。提到顶栏常驻：

- 未设目标：虚线描边 chip，`--ft-text-tertiary`，文案「未设定阶段目标 · 点击设定」。
- 已设目标：实心 chip，`--ft-accent-soft` 底 + `--ft-accent` 左侧 2px 竖条，显示 `goal.outcome`（超 40 字省略），右侧 mono 小字显示剩余天数（`剩 12 天`）。长期目标显示「长期」。
- 已过期：底色换 `--ft-warn-soft`，右侧显示「已过期」，并在 chip 右上角一个 4px `--ft-warn` 圆点。
- 点击：展开一个 420px 宽的浮层，可编辑 outcome / kind / deadline / constraints / exclude，底部「保存为新版本」与「清除目标」。保存时旧目标进 `goal_history`（沿用现有 `useUserProfile` 逻辑）。
- hover：显示 tooltip，列出 constraints 与 exclude。

**右区**
- 撤销/前进：合并为一个分段控件（两个相连按钮，中间 1px 分隔）。可用时显示动作名（截断 12 字），不可用时降为 `--ft-text-tertiary` 且不可点。右键任一侧弹出对应的历史列表（沿用现有 `HistoryPanel` 行为）。
- 备份状态：一个 8px 圆点 + 「数据」。超过 7 天未手动导出时圆点为 `--ft-warn` 并带 2.4s 呼吸。点击打开抽屉「数据」tab。
- 账户菜单：用户头像圆（邮箱首字母，底色由邮箱 hash 映射到分支色板）。菜单项：主题（深色/浅色/跟随系统，分段控件）、AI 记忆、推荐记录、会话历史、数据备份、退出登录。前四项都是打开右抽屉对应 tab。

顶栏**不再**包含：展开/折叠、密度、全局视图、末端视图、审计、对话开关。它们分别去了左导轨和画布控件簇。

---

## 5. 左导轨

宽 56px，纵向排列 40×40 的图标按钮，图标 20px。使用 `lucide-react`（round cap / 1.5 stroke，风格契合；tree-shaking 后体积可忽略），品牌标记与种子插画手写内联 SVG。

**上组 —— 主视图模式（互斥单选）**

| 图标 | 名称 | 说明 | 快捷键 |
| --- | --- | --- | --- |
| `Network`（旋转 90°）或自绘枝形 | 结构 | 树画布。**默认** | `1` |
| `Sun` | 聚焦 | 今日 3 件事全屏视图 | `2` |
| `ListChecks` | 清单 | 原「末端视图」，改名 | `3` |
| `BookOpen` | 回顾 | 周回顾阅读视图 | `4` |

选中态：图标 `--ft-accent`，按钮底 `--ft-accent-soft`，左侧一条 2px × 20px 的 `--ft-accent` 竖条贴着导轨左边缘。
未选中：`--ft-text-tertiary`，hover 变 `--ft-text-secondary` + `--ft-surface-hover` 底。
所有按钮 hover 200ms 后向右弹出 tooltip（名称 + 快捷键）。

**分隔线**（`--ft-border-subtle`，左右各留 12px）

**中组 —— 抽屉快捷**

| 图标 | tab | 快捷键 |
| --- | --- | --- |
| `MessageSquare` | 对话 | `C` |
| `Inbox` | 待确认（带角标） | `I` |
| `Activity` | 审计 | `A` |

点击 = 打开抽屉并切到该 tab；再次点击同一个 = 收起抽屉。
待确认角标：右上角 `--ft-accent` 实心圆，内嵌 `--ft-text-inverse` 10px mono 数字；> 9 显示 `9+`。数字从 0 变正数时播放一次 `--ft-dur-3` 的 scale 0.6→1 生长动画。

**下组（贴底）**
- 主题切换（`Moon` / `Sun`）
- 用户头像（与顶栏账户菜单同一个菜单）

> 顶栏与左导轨的分工必须严格：**顶栏 = 我是谁、我在追什么、我刚做了什么；左导轨 = 我要去哪、我要打开什么；画布控件簇 = 我要怎么看。** 任何新功能按这三问归位，不允许再往顶栏堆。

---

## 6. 画布控件簇与图例

### 6.1 控件簇（画布右下角，距边缘 16px）

浮起卡片，`--ft-surface-raised` + `--ft-border` + `--ft-shadow-md` + `--ft-radius-lg`，默认收起为一条竖列：

```
[ + ]
[ 78% ]   ← 点击复位到 100%
[ − ]
─────
[ ⤢ ]     ← 全局（fit to view）
[ ⋯ ]     ← 展开更多
```

点 `⋯` 向左展开第二列（`--ft-dur-2` / `--ft-ease-settle`，从右向左展开）：

- 展开全部 / 折叠全部
- 密度分段控件：疏 · 中 · 密
- **图层开关**（三个 toggle）：期限弧、年轮、分数标签

图层开关是给「今天只想看结构」的场景准备的，状态持久化到 `localStorage.ft_layers`。三个默认全开。

### 6.2 图例（画布左下角，距边缘 16px）

这是本次设计的**必需组件**，不是可选装饰。三通道不解释没人能读懂：

```
┌─────────────────────────────┐
│ 怎么读这棵树            [收起] │
│                             │
│  ●  大小·辉光   现在多值得做   │
│  ╱   枝干粗细   这条路通向的意义│
│  ◎  外圈年轮   已经投入多少   │
│                             │
│  ⌒  外弧       期限压力       │
│  ▲  上方芽尖    你标了紧急     │
│  ○  虚线空心    已暂停        │
│  ✓  勾          已完成        │
└─────────────────────────────┘
```

每行左侧是**真实渲染的迷你 SVG 示例**（不是文字符号），右侧 `--ft-text-xs` 说明。首次使用默认展开，收起状态持久化到 `localStorage.ft_legend`。收起后变成一个 28px 的 `?` 圆钮。

hover 某一行时，画布上对应通道**高亮其余降透明到 0.25**，持续 hover 期间。这让图例可教学。

### 6.3 今日条（画布左上角，距边缘 16px）

替代现有的浮动 `TodayCard`。收起态是一行胶囊：

```
[ ☀ 今天 3 件事 · 已完成 1 ]
```

- 点击展开为 300px 宽的浮层卡片，列出 3 件事（同现有 TodayCard 内容），可勾选、可移除、可 hover 高亮到树。
- 展开态可「钉住」（图钉图标），钉住后不因点击画布收起。
- **不可永久关闭**。用户能收起，但胶囊常驻。理由：这是产品要回答的第一个问题，不该能被永久隐藏。
- 未生成时显示 `[ ☀ 生成今天的 3 件事 ]`，点击触发 `dailyFocus.generate`。
- 全部完成时胶囊变 `--ft-accent-soft` 底，文案「今天 3 件都完成了」。

---

## 7. 树画布可视化（核心）

### 7.0 实现约束（必读）

`src/components/Tree/TreeView.jsx` 当前 1372 行，其中包含拖拽命中检测、SVG 原生事件代理、D3 zoom 协同、右键菜单、末端加号手势、reorder 判定等大量已验证的交互逻辑。

**硬性要求：不重写交互层，只替换渲染层。**

把渲染抽成纯函数模块，`TreeView.jsx` 调用它们：

```
src/components/Tree/render/
  branchPath.js      锥形枝干路径生成
  nodeVisual.js      节点半径、辉光、状态形状
  growthRings.js     年轮
  dueArc.js          期限弧
  urgentMark.js      紧急芽尖
  labels.js          标签排版与避让
  defs.jsx           <defs> 共享滤镜与渐变
```

每个模块输入是「节点数据 + priority meta + 主题令牌值」，输出是 SVG 属性对象或 path 字符串，**不碰 DOM 事件**。

以下交互在重做后必须逐条验证仍然可用，任何一条丢失即验收不通过：

1. 单击选中节点
2. 双击折叠/展开
3. 右键打开上下文菜单
4. 悬浮实心圆点时圆点放大并出现加号，点加号新增子节点
5. 按住圆点拖拽整条分支到另一节点下（禁止拖到自己的子孙）
6. 拖拽时显示预览曲线与节点数量徽章
7. 向右拖出 ≥28px 松手创建同级/子节点
8. 兄弟节点之间的 reorder（上/下插入）
9. D3 zoom（滚轮/捏合）与 pan（二指/中键拖拽）
10. 拖拽阈值 4px、拖拽期间禁用加号
11. drop 先 DOM 命中检测再树坐标距离兜底

### 7.1 三通道映射

设 `d = directPriority/100`，`b = branchPriority/100`，`c = cultivationScore/100`，均 ∈ [0,1]。

#### 通道一：directPriority → 节点大小 + 辉光（「现在」）

基础半径按 type 保持层级可读：

```
base = { project: 16, category: 10, task: 6.5 }[type]
r    = base * (0.82 + 0.50 * d^0.85)
```

得到范围：project 13.1→21.1，category 8.2→13.2，task 5.3→8.6。

辉光（只在 `d ≥ 0.55` 时出现）：

```
g = (d - 0.55) / 0.45              // 0→1
辉光圆 r_glow   = r * (1.9 + g * 1.3)
      opacity   = 0.10 + g * 0.34
      fill      = 该节点的分支色
      filter    = url(#ft-glow)     // 共享 feGaussianBlur stdDeviation=3.5
```

**性能约定**：只允许 `<defs>` 中存在**一个** `#ft-glow` 滤镜实例，所有辉光圆共用。强度靠 `r` 与 `opacity` 调，不得为每个节点生成滤镜。当可见节点数 > 400 时，辉光降级为无滤镜的 `radialGradient` 填充圆。

#### 通道二：branchPriority → 锥形枝干（「未来」）

现有实现是等宽 `stroke`。改为**锥形填充路径**：枝干从父端向子端收细，这既符合真实树的形态，也正确表达了「意义沿关键路径向根部汇聚」。

宽度映射（每个节点算一个自身宽度）：

```
w(node) = 1.2 + b^1.30 * 13.0        // 1.2 → 14.2
```

一条 link（parent → child）的路径生成算法（`branchPath.js`）：

1. 取 D3 已有的水平三次贝塞尔控制点：`P0 = (px, py)`，`C0 = (mx, py)`，`C1 = (mx, cy)`，`P1 = (cx, cy)`，其中 `mx = px + (cx - px) * 0.5`。
2. 采样 `N = 24` 个 `t ∈ [0,1]`，求曲线点 `B(t)` 与切线 `B'(t)`，法线 `n(t) = normalize(perp(B'(t)))`。
3. 宽度沿曲线插值，**父端更粗、子端更细**，并在末端略微内收：
   ```
   w(t) = lerp(w_parent, w_child, smoothstep(t)) * (1 - 0.12 * t^3)
   ```
4. 上沿点 `B(t) + n(t) * w(t)/2`，下沿点 `B(t) - n(t) * w(t)/2`。
5. 上沿正序 + 下沿逆序闭合成一条 `path`，`fill = 子节点的分支色`，`stroke = none`。

不透明度：

```
fill-opacity = 0.34 + b * 0.48        // 0.34 → 0.82
```

状态调整：`done` 整段 `fill-opacity *= 0.5` 且走 `shadeBranchColor` 的 done 分支；`dormant` `*= 0.42`。

关键路径提示：当某条 link 的 `child.branchPriority` 恰好是父节点 `branchPriority` 的来源（即 `upwardCritical` 由该子节点决定）时，在枝干中心叠加一条极细的亮线（宽 1，`--ft-text-primary`，opacity 0.10）。这让「枝干的意义从哪来」在画布上可见，不必打开审计。

#### 通道三：cultivationScore → 年轮（「过去」）

培育程度不是理性优先级，**绝不能**用大小或亮度表达，否则和前两通道混淆。用节点外的同心细环，隐喻树的年轮：

```
仅当 c ≥ 0.15 时绘制
rings = clamp(round(c * 4), 1, 4)
第 i 环 (i = 1..rings)：
  r_i       = r + 4 + i * (3 + c * 3)
  stroke    = 分支色
  stroke-width = 0.75
  fill      = none
  opacity   = 0.18 * (1 - (i-1)/rings) * (0.4 + c * 0.6)
```

年轮是**极低对比的背景纹理**，远看是一层柔和的涟漪，凑近或 hover 才数得清。这正是它应有的份量：可见、可查、不抢戏。

**性能降级**：可见节点数 > 300 时，`rings` 强制为 1，用 `opacity` 单独编码 `c`。

节点 hover 或选中时，年轮 opacity 统一 ×2.2，让用户能读出圈数。

**晨纸主题校对（实测）**：浅色画布上的辉光不使用分支色亮晕。`node-direct-glow` 改用 `--ft-text-primary` 暗晕，`stdDeviation = 3.5`、半径仍按 `r × 1.9..3.2`，opacity 乘以 `0.58`，最终范围约 `0.06..0.26`；年轮改用 `--ft-text-secondary` 描边，opacity 乘以 `1.65` 并封顶 `0.85`。这样 direct 的分支色仍由节点本体表达，暗晕只表达强度，不会在米白底上泛白。

对 `--ft-surface` 的实际对比度（相对 luminance ratio）为：林夜 `text-primary 14.72:1 / secondary 7.86:1 / tertiary 4.06:1`；晨纸 `text-primary 14.16:1 / secondary 5.90:1 / tertiary 3.03:1`。分支色对各自画布的最低实测值为林夜 `4.33:1`、晨纸 `4.02:1`，均高于本节要求。

### 7.2 状态叠加（非分数）

#### 期限弧

节点外的一段圆弧，起点 12 点方向顺时针。弧越满越紧迫，像沙漏：

| `dueState` | 角度 | 颜色 | 动画 |
| --- | --- | --- | --- |
| `overdue` | 360° | `--ft-danger` | 1.6s 呼吸，opacity 0.55↔1.0 |
| `today` | 300° | `--ft-danger` | 无 |
| `three_days` | 210° | `--ft-warn` | 无 |
| `week` | 120° | `--ft-warn` | 无 |
| `later` / 无 | 不绘制 | | |

```
半径 = r + 3.5, stroke-width = 2, stroke-linecap = round, fill = none
```

沿用现有 `getNodeDueState()`，不改逻辑。

#### 紧急标记

`current_priority === 'urgent'` 时，节点正上方 `r + 7` 处画一个 6×6 的**芽尖**（一个顶点向上的窄三角，底边内凹成叶形），`fill = --ft-text-primary`，带 `--ft-shadow-sm`。

用形状而不是颜色，因为颜色通道已被「归属」占满。

#### 状态形状

| status | 表现 |
| --- | --- |
| `active` | 实心圆 |
| `dormant` | **空心**：`fill = --ft-canvas`，`stroke = 分支色`，`stroke-width = 1.5`，`stroke-dasharray = "2 2"`；整体 opacity 0.38 |
| `done` | 实心但去饱和，opacity 0.45；`r ≥ 8` 时圆内画一个 `--ft-canvas` 色的对勾（2px round cap），`r < 8` 时省略对勾 |

`dormant` 用虚线空心是关键的第二通道——只降透明度在低对比屏幕上和 done 区分不开。

#### 选中与高亮

- **选中**：节点外 `r + 5` 处一圈 2px `--ft-accent` 实线环；同时画布上以该节点为中心叠加一个半径 180px 的极淡 radial gradient 提亮（`--ft-accent` 4% → 0%），做成「聚光」。
- **高亮**（来自对话中的任务名 hover、今日条 hover）：`--ft-ai` 色的双环脉冲，1.2s 循环两次后停止；同时若节点在视口外，画布平滑平移使其居中（`--ft-dur-4` / `--ft-ease-settle`）。

### 7.3 标签

- **显示规则**：现有 `density`（疏=仅 project / 中=非 task / 密=全部）保留为用户覆盖，但增加自动规则并取并集：
  - `zoom < 0.5` → 强制仅 project
  - `zoom > 1.4` → 强制全部显示
- **位置**：节点右侧 `r + 10`，垂直居中，`text-anchor: start`。
- **主标签**：`--ft-text-base` / 400 / `--ft-text-primary`。为保证压在枝干上仍可读，使用 `paint-order: stroke fill` + `stroke: var(--ft-canvas)` + `stroke-width: 3` + `stroke-linejoin: round`。**不要**用矩形底板，会破坏有机感。
- **副标签**（仅 hover / 选中 / 「分数标签」图层开启时）：主标签下方 2px，`--ft-font-mono` / `--ft-text-2xs` / `--ft-text-tertiary`，格式 `d 82 · b 91 · c 47`。三个数字之间用 `·` 分隔，字母是通道首字母，与图例对应。
- 超长名称截断到 22 个中文字符 + `…`，完整名在 tooltip。

### 7.4 空状态

替换现有的 🌱 emoji：

- 居中一株**种子/嫩芽**的手绘线条 SVG（120×120，`--ft-accent` 描边 1.5，两片子叶），带 4s 极缓的呼吸缩放（0.98↔1.02）。
- 下方衬线大字（`--ft-font-serif` / `--ft-text-2xl` / `--ft-text-primary`）：**「先说说你在忙什么」**
- 再下一行 `--ft-text-base` / `--ft-text-secondary`：「把脑子里的事一股脑说出来，我帮你分成主线。」
- 再下方三个可点击的示例 chip，点击直接填入对话输入框并聚焦（不自动发送）：
  - 「我现在同时在做三件事，有点乱」
  - 「帮我把这段项目描述整理成结构」
  - 「我这周该先做什么」

### 7.5 生长动效

这是「有机」气质的核心，不允许省略。

- **新增节点**：先由父节点沿枝干路径「长出」枝干（path 用 `stroke-dasharray` 描边动画，`--ft-dur-4` / `--ft-ease-grow`），枝干到位后节点圆从 `r=0` 弹到 `r`（`--ft-dur-3` / `--ft-ease-grow`），最后标签淡入。
- **删除节点**：反向，节点先收缩到 0，枝干再从末端收回父节点。
- **完成节点**：对勾以 `stroke-dashoffset` 画出（`--ft-dur-2`），同时节点去饱和到 done 状态。
- **布局重排**（展开/折叠/移动分支）：所有节点与枝干走 `--ft-dur-4` / `--ft-ease-settle` 的 D3 transition，不是瞬间跳变。
- **分数变化**（应用优先级分析后）：受影响节点的半径与辉光走 `--ft-dur-4` 过渡，并在变化最大的 3 个节点上播放一次 `--ft-accent` 的单次脉冲环。让「确认后发生了什么」看得见。

---

## 8. 右抽屉

### 8.1 结构

```
┌────────────────────────────────────┐
│ ⇔ │ 对话  待确认②  详情  审计   ✕ │  ← 44px，⇔ 是拖拽把手
├────────────────────────────────────┤
│                                    │
│            tab 内容                 │
│                                    │
└────────────────────────────────────┘
```

- **固定 tab**：对话、待确认、详情、审计。始终存在，顺序固定。
- **临时 tab**：AI 记忆、推荐记录、会话历史、数据备份。从账户菜单打开时插入到审计右侧，带 `✕` 可关闭，同一时刻最多存在 2 个临时 tab（超出时关掉最旧的）。
- **详情 tab**：无选中节点时显示为禁用态（`--ft-text-tertiary`，不可点，文案 `详情`）。选中节点时自动激活并**自动切到该 tab**——除非用户在本次会话里手动切走过（用一个 `userPinnedTab` 标志记住，避免打断正在打字的对话）。
- **待确认 tab**：有未处理项时标题右侧带角标。
- 左边缘 6px 的拖拽把手，`col-resize` 光标，hover 时显现一条 2px `--ft-accent` 线。双击把手复位到 380px。
- 抽屉展开/收起：宽度 `--ft-dur-2` / `--ft-ease-settle`，内容 opacity 同步。

### 8.2 待确认收件箱（本次新增，最重要的新组件）

**问题**：AI 的 `draft_actions`、`proposed_panel_changes`、`goal_analysis`、`node_priority_proposals` 目前只存在于聊天流里的卡片中，滚上去就找不回来。而 `PRODUCT_SPEC.md` §7 的第 3 条原则明确要求「用户负责确认重要决策」——需要确认的东西必须有常驻位置。

**条目类型**

| 类型 | 来源字段 | 图标 | 说明 |
| --- | --- | --- | --- |
| 结构草案 | `draft_actions` / `proposed_panel_changes` | `GitBranch` | 建议新增/调整的节点 |
| 目标变更 | `goal_analysis` | `Target` | 新的阶段目标提案 |
| 优先级信号 | `node_priority_proposals` | `Gauge` | 节点的四个语义信号 |

**条目卡片**（`--ft-surface-raised`，`--ft-radius-lg`，左侧 3px `--ft-ai` 竖条）

收起态一行：图标 + 摘要（如「新增 3 个节点到「熊猫团团」」）+ 来源时间 + 展开箭头。
展开态：

- **结构草案**：树状缩进预览，每个待建节点显示 `类型 · 名称 · 父节点`。已存在同名同父节点的条目标灰并标注「面板中已有」（沿用现有去重检查）。逐条可勾选。
- **目标变更**：新旧目标并列 diff。字段级展示 outcome / kind / deadline / constraints / exclude，变化的字段底色 `--ft-accent-soft`。
- **优先级信号**：见下。

**优先级信号的编辑控件**（这是本产品最有价值的交互，要做扎实）

每个节点一张子卡片：

```
┌──────────────────────────────────────────────┐
│ ● 完成客户回款              置信度 ████░ 80%  │
├──────────────────────────────────────────────┤
│ 目标契合   无关 ─────────●── 必需      0.90  │
│ 必要性     可选 ──────────● 不可绕过   1.00  │
│ 延误损失   很小 ────────●─── 很大      0.85  │
│ 关系类型   [必需] 支撑  普通  探索           │
├──────────────────────────────────────────────┤
│ "回款是当前现金流目标不可绕过的末端步骤"        │
├──────────────────────────────────────────────┤
│ 当前优先级 46  →  应用后 78   ↗ +32          │
│                       [ 否决 ]  [ 采纳 ]      │
└──────────────────────────────────────────────┘
```

- 三个 0–1 信号用 slider，步长 0.05，轨道上有 5 个刻度标签（无关/弱/中/强/必需 等，按信号语义各自命名）。滑块初始位置是模型给的值；用户改动后滑块变 `--ft-accent`，并在标签旁显示 `模型 0.90` 的原值以便对照。
- `relation_type` 用分段控件（枚举取值以 `server/agent.js` 中的定义为准）。
- `confidence` 只读，用一条 4px 的条 + 百分比。低于 0.5 时条为 `--ft-warn` 并在卡片顶部加一行「模型对这条不太确定」。
- **分数变化预览**：`src/lib/priorityEngine.js` 在前端，因此可以本地实时计算「应用这些信号后 `directPriority` 会变成多少」。每次滑动 slider 都要重算并更新预览（debounce 120ms）。这是让用户真正理解引擎的关键，**必做**。
- 节点名左侧是它的分支色圆点，点击节点名 = 在树上选中并居中该节点。

**批量操作**（卡片底部固定条）：「全部采纳」「全部否决」，以及一个 `n / m 已选` 计数。

**已处理区**：应用或否决后条目移入底部的「已处理」折叠区，保留 7 天，显示处理时间与结果。这让用户能回溯「我当时确认了什么」，也对应 `priority_analysis_runs` 的审计目的。

**空状态**：一个安静的插画 + 「没有待确认的提案。和 AI 聊聊你在忙什么，它会把建议放到这里。」

**与聊天流的关系**：聊天流里对应位置保留一张**引用卡片**（一行：「产生了 3 条待确认提案 →」），点击跳到收件箱并高亮对应条目。提案的完整交互只在收件箱里做一次，不在两处重复实现。

### 8.3 对话 tab

- **AI 消息不再是气泡**。改为：左侧 2px `--ft-ai` 竖线 + 无底色段落，`--ft-text-md` / 1.75。长文本可读性提升明显，且视觉上像「批注」。
- **用户消息保留气泡**：右对齐，`--ft-accent-soft` 底，`--ft-radius-xl`（右下角 `--ft-radius-sm`），最大宽度 82%。
- **元信息行**：模型 / 耗时 / token / 费用 / 上下文策略，`--ft-font-mono` / `--ft-text-2xs` / `--ft-text-tertiary`，单行，默认只显示耗时；hover 消息时其余淡入。
- **思考（`ThinkingCard`）**：默认折叠成一行「思考过程 · 6 条」，点击展开。展开后按 `PRODUCT_SPEC.md` §4.2 的字段分组呈现，`situation_map` 用列表，`open_questions` 用问号图标列表。**注意**：按 `HANDOFF.md` 的口径，只呈现对用户有决策价值的内容，模型自用的脚手架字段不展示。
- **任务名引用**：`「xxx」` 渲染成 inline chip——分支色 6px 圆点 + 名称 + `--ft-surface-hover` 底 + `--ft-radius-sm`。hover 高亮树上节点（现有行为），**新增**：单击选中该节点并让画布居中。
- **状态行**（`[OK]` / `[目标]` / `[-]`）：保留识别逻辑，改为左侧色条 + 图标的行内提示条，分别用 `--ft-accent` / `--ft-accent` / `--ft-warn`。
- **等待态**：不用 `...`。用一条 2px 的水平线从左向右「生长」并循环（`--ft-dur-4`，`--ft-ease-grow`），色 `--ft-ai`。旁边显示已等待秒数（mono）。有排队时显示 `队列 2`。
- **输入区**：`--ft-surface-sunken` 底，`--ft-radius-lg`，自动增高（最小 2 行，最大 10 行）。底部一行 32px 工具条：模型选择（下拉）、上下文策略指示、右侧发送/取消按钮。`/` 开头时在输入框上方弹出命令提示列表（`/目标`、`/清空` 等现有命令）。
- 顶部的目标条移除（已提到顶栏），原位置改为一行极简的会话信息：`本次会话 · 12 条 · 新会话`。

### 8.4 详情 tab

沿用现有 `NodeDetailPanel` 的全部字段与自动保存逻辑（1200ms debounce、Esc 还原、Ctrl/Cmd+S 立即保存），仅重排视觉：

顺序：**面包屑路径 → 标题 → 三分数条 → 状态 → 当下优先级 → 目标完成日期 → 详细想法 → AI 标注**

- **面包屑**：根 → … → 当前节点，每级可点击跳转选中。分支色圆点 + 名称，`--ft-text-xs`。
- **三分数条**（新增，重要）：三行，每行是 `名称 + 4px 进度条 + mono 数值`，颜色分别用节点分支色、分支色 70% 不透明、`--ft-text-tertiary`。右侧一个「为什么」链接，点击切到审计 tab 并定位到该节点。
- **状态**：三个分段按钮（进行中/完成/暂停），选中态用 `--ft-accent-soft` + `--ft-accent` 边。
- **当下优先级**：五个分段按钮。`urgent` 选中时用 `--ft-text-primary` 边框 + 芽尖图标，与树上的编码一致。
- **目标完成日期**：原生 `date` 输入 + 右侧实时显示 `dueState.label`，颜色与树上期限弧一致（`--ft-danger` / `--ft-warn`）。
- **详细想法**：`--ft-text-md`，最小高度 220px，可拖高。
- **AI 标注**（新增只读区）：显示 `annotations.priority_analysis` 的确认时间、目标版本、是否已过期。过期时显示 `--ft-warn` 的「分析已过期，当前使用本地信号」+ 一个「重新分析此节点」按钮。
- 保存状态指示移到 tab 标题右侧的小圆点：保存中 = `--ft-warn` 呼吸，已保存 = `--ft-accent` 后淡出，失败 = `--ft-danger` 常亮 + 可点击重试。

### 8.5 审计 tab

现有 `PriorityDebugPanel` 从「调试工具」升级为「**解释器**」。改名为「审计」→ 界面标题写「为什么是这个分数」。

结构：

1. **对象选择器**：默认跟随树上选中的节点；顶部可切换为「整棵树」。
2. **三分数大字**：mono / `--ft-text-xl`，三个并排，下方各一行说明。
3. **directPriority 瀑布图**（引擎已输出 `contributions` 数组，直接用）：
   - 横向条形瀑布，从基线开始，每个因子一段：正贡献 `--ft-accent`，负贡献 `--ft-danger`，最终值一段实心。
   - 每段标注因子名与 `±n`。hover 显示该因子的原始输入值。
   - 状态惩罚（done → 0，dormant → ×0.28）作为独立的最后一段，标注清楚。
4. **branchPriority 关键路径**：一条水平的 chip 链，从当前节点指向决定其 `upwardCritical` 的那个后代，每个 chip 可点击选中。链上标注每一跳的 `relation_type` 与传播系数。这直接可视化了「意义从哪来」。
5. **cultivationScore 构成**：四项（自身证据 42% / 结构 32% / 完成 14% / 近期活跃 12%）的堆叠条 + 数值。
6. **分析新鲜度**：显示分析所用的目标版本与节点指纹，过期时显著提示，并提供「补充分析缺失节点」与「全树重新分析」两个按钮（沿用现有 `requestPriorityAnalysis` 的 quick / full 模式）。
7. **整棵树模式**：一个可排序表格（节点 / direct / branch / cultivation / 状态 / 期限），支持按任意列排序，行可点击定位。

---

## 9. 其他主视图

### 9.1 聚焦（今日）

全屏，内容居中，最大宽度 720px。

- 顶部：`--ft-font-serif` / `--ft-text-2xl` 的一句话，按时段变化（「早上好，今天从这里开始」/「下午了，还有两件」/「晚上了，收个尾」）。
- 三张卡片纵向排列，每张：时段徽章（早上/下午/晚上/任意）、任务名（`--ft-text-lg`）、所属项目（分支色点 + 名）、AI 给的理由（`--ft-text-base` / `--ft-text-secondary`）、右侧大号勾选圆。
- 完成时：卡片走一次「落定」动效（轻微下沉 + 去饱和），移到列表底部，不消失。
- 底部：`已完成 1 / 3` 进度 + 「重新生成」+ 「去看树」。
- 未生成时是一个居中的生成按钮 + 说明。

### 9.2 清单（原「末端视图」）

保留现有分组（进行中 / 暂停中 / 已完成）与排序逻辑，改名为「清单」，并补齐三处：

- **顶部控制条**：分组方式（按状态 / 按项目 / 按期限）、排序（直接优先级 / 枝干意义 / 期限 / 培育度）、过滤（只看今日推荐 / 只看有期限的）。
- **每行**：最左一条 3px 分支色竖条（归属，替代现在的小圆点）；中间任务名 + 面包屑；右侧**三个 3px 微条**表示三个分数（不是现在的一个条），宽度 40px，纵向堆叠，hover 显示数值。
- **行内编辑**：状态圆圈直接切换（现有）；**新增**右侧 hover 出现的优先级与日期快捷设置，不必打开详情。
- 空状态与树的空状态呼应。

### 9.3 回顾（周回顾）

这是产品里唯一的「慢阅读」场景，单独排版：

- 居中，最大宽度 680px，上下各留 64px。
- `opening` 与 `closing` 用 `--ft-font-serif` / `--ft-text-lg` / 1.9 行高 / `--ft-text-secondary`。
- `wins` / `patterns` / `challenges` / `proposals` 四段各有一个小标题（`--ft-text-sm` / 500 / `--ft-text-tertiary` / 字间距 0.08em），正文 `--ft-text-md` / 1.8。
- `proposals` 中若引用了真实节点，渲染成可点击 chip（同 §8.3）。
- 顶部一行：本次回顾的时间范围 + 「生成新回顾」；历史回顾用一个左侧的日期列表切换。
- 无回顾时：说明距上次回顾还有几天，以及一个「现在就生成」按钮。

---

## 10. 可访问性

1. **对比度**：`--ft-text-primary` 对 `--ft-surface` ≥ 7:1；`--ft-text-secondary` ≥ 4.5:1；`--ft-text-tertiary` ≥ 3:1。两个主题都要满足。分支色对画布底 ≥ 3:1（作为大色块使用）。
2. **不只靠颜色**：
   - `dormant` = 虚线空心（形状）
   - `done` = 对勾 + 删除线（形状）
   - `urgent` = 芽尖（形状）
   - 期限 = 弧长（几何）+ 详情里的文字
   - 分数 = 尺寸/粗细（几何）+ 副标签数字（文字）
   - 归属 = 颜色 + 清单/详情里的项目名文字
3. **焦点可见**：所有可交互元素 `:focus-visible` 显示 2px `--ft-accent` 环 + 2px offset。禁止 `outline: none` 而不给替代。
4. **键盘导航**（新增能力）：
   - 全局：`1`–`4` 切主视图，`C`/`I`/`A` 切抽屉 tab，`Esc` 收起抽屉/关闭浮层，`/` 聚焦对话输入框。
   - 树画布：`Tab` 进入画布后，方向键在树上移动选择（上下=同级兄弟，左=父，右=第一个子），`Enter` 展开/折叠，`Space` 打开详情，`Delete` 删除（走现有确认），`Ctrl/Cmd+Z` 撤销。
   - 所有快捷键在 `isTypingTarget()` 为真时全部禁用（现有逻辑保留并扩展）。
   - 一个 `?` 快捷键弹出快捷键速查表浮层。
5. **语义标记**：树画布 SVG 加 `role="tree"`，节点组 `role="treeitem"` + `aria-expanded` + `aria-selected` + `aria-label`（节点名 + 类型 + 状态 + 三个分数的口语化描述）。抽屉 `role="complementary"`，tab 用 `role="tablist"/"tab"/"tabpanel"`。
6. **动效**：遵守 `prefers-reduced-motion`（见 §2.6）。
7. **点击目标**：所有可点击区域实际命中面积 ≥ 32×32（树上的小节点通过透明 hit area 扩大，现有 `NODE_HIT_PADDING` 机制保留并调到 8）。

---

## 11. 实现约定与执行顺序

### 11.1 文件组织

```
src/
  styles/
    tokens.css            设计令牌（唯一允许写颜色字面量的地方）
    base.css              全局 reset、字体、滚动条
  components/
    Shell/                AppShell / TopBar / LeftRail / Drawer / GoalChip
    Tree/
      TreeView.jsx        交互层（不重写，只改渲染调用）
      render/             纯渲染模块（见 §7.0）
      CanvasControls.jsx  控件簇
      Legend.jsx          图例
      TodayPill.jsx       今日条
      ContextMenu.jsx     保留
    Drawer/
      ChatTab.jsx  InboxTab.jsx  DetailTab.jsx  AuditTab.jsx
      inbox/              提案卡片、信号滑块、分数预览
    Views/
      FocusView.jsx  ListView.jsx  ReviewView.jsx
    ui/                   Button / Segmented / Slider / Chip / Tooltip / Badge / EmptyState
    icons/                品牌标记与手绘插画（lucide 之外的）
```

`src/components/ui/` 是本次必须建立的基础组件层。禁止再在业务组件里手写按钮和输入框样式。

### 11.2 依赖

- **允许新增**：`lucide-react`（图标）。
- **禁止新增**：动画库（用 CSS + D3 transition 已足够）、UI 组件库、CSS-in-JS、图表库。
- 继续使用 `d3` 做树布局与 zoom，不替换。

### 11.3 数据契约（不改）

本次是纯前端表现层重做。以下**不允许改动**：

- `server/` 下任何文件
- `src/lib/priorityEngine.js` 的计算逻辑与既有计算结果（仅允许增加解释性输出字段，不改变现有结果）
- `src/lib/priorityAnalysis.js`、`src/lib/intentClassifier.js`
- `src/hooks/` 下各 hook 的对外接口（内部可重构，但签名保持）
- 数据库结构与 `src/lib/supabase.js`

`src/lib/treeUtils.js` 中的**视觉映射函数**（`getNodeRadius`、`getPriorityLinkStrokeWidth`、`getNodeColor`、`getLinkStrokeWidth`）由 §7.1 的新公式替代，迁移到 `src/components/Tree/render/`；`treeUtils.js` 中的数据函数（`flatToTree`、`findNodeById`、`getNodeDueState`、`getDerivedWeightMeta*`、`flattenTree`、`PRIORITY_*`）保留不动。旧的视觉函数按 `AGENTS.md`「不保留向后兼容」的原则**删除**，不留别名。

### 11.4 执行顺序

用户要求一次性完整重做，不分期发布。但执行必须有序，**每一步结束时 `npm run build`、`npm test`、`npm run lint` 都要通过，主流程（登录 → 看树 → 对话 → 改节点）不能断**。这不是分期交付，是不允许把树砸烂之后再慢慢修。

1. **令牌与换肤**：建 `tokens.css` + `base.css`，实现主题切换，把 `src/` 下所有硬编码颜色替换成令牌。此步结束时布局不变但整体已换色。
2. **基础组件层**：建 `src/components/ui/`，把现有散落的按钮/输入/分段控件收敛进来。
3. **布局骨架**：`AppShell` / `TopBar`（含目标 chip）/ `LeftRail` / `Drawer`。把现有面板原封不动塞进 Drawer 的 tab 里，先解决「三面板并排挤压树」的问题。
4. **树渲染层**：抽出 `render/` 模块，实现三通道（锥形枝干 → 节点大小辉光 → 年轮）、期限弧、紧急芽尖、状态形状、标签排版。**逐条验证 §7.0 的 11 项交互**。
5. **画布外围**：控件簇、图例（含 hover 教学联动）、今日条、空状态。
6. **待确认收件箱**：条目模型、三类卡片、信号滑块、**分数变化实时预览**、批量操作、已处理区、与聊天流的引用卡片联动。
7. **对话与详情重排**：AI 消息去气泡、思考折叠、任务 chip 可点定位、输入区、详情 tab 三分数条与 AI 标注区。
8. **审计解释器**：瀑布图、关键路径 chip 链、培育构成、整树表格。
9. **其余主视图**：聚焦、清单、回顾。
10. **生长动效**：新增/删除/完成/重排/分数变化五组动效。
11. **可访问性收尾**：对比度校验（两个主题）、焦点环、树的键盘导航、ARIA、reduced-motion、快捷键面板。
12. **晨纸主题校对**：逐屏检查浅色主题，重点是树画布、辉光、年轮在浅底上的可读性（辉光在浅底需改为「暗晕」而非亮晕，具体值在实现时定，写回本文）。

### 11.5 文档义务

按 `AGENTS.md`：完成后更新 `HANDOFF.md` 的架构现状与下一步；把重做过程中做出的设计决策、试过但放弃的方案追加到 `MEMORY.md`。实现中若偏离本文任何规范值，先改本文再改代码。

---

## 12. 不做清单

明确排除，避免范围蔓延：

- 移动端与触屏专用布局（`PRODUCT_SPEC.md` §6 已列为 V1 不做）
- 引入任何 UI 组件库或动画库
- 节点自由拖拽定位（树布局仍由 D3 计算）
- 树的 Canvas/WebGL 渲染（SVG 足够；性能靠 §7.1 的降级阈值）
- 多用户协作的任何界面
- 用户自定义主题色 / 自定义分支色板
- 服务端与算法层的任何改动

---

## 13. 验收清单

以下每一条都会被逐项检查，任一不通过即打回：

**基础**
- [ ] `npm run build` / `npm test`（13 项）/ `npm run lint` 全部通过，lint 不新增错误
- [ ] `src/` 下除 `styles/tokens.css` 与 `lib/branchPalette.js` 外，无颜色字面量
- [ ] 深浅两个主题都完整可用，切换无闪烁，刷新后保持

**布局**
- [ ] 任何操作路径下，右侧同时只有一个面板，树的宽度不会被两块以上面板挤压
- [ ] 阶段目标常驻顶栏，未设定/已设定/已过期三态正确
- [ ] 顶栏不含视图控制类按钮；画布控件簇不含导航类按钮

**树可视化**
- [ ] 三个分数分别可见：节点大小+辉光、锥形枝干、年轮。构造一棵测试树，让三个分数互相背离（高 direct 低 branch、低 direct 高 cultivation 等），三者仍能各自读出
- [ ] 枝干是锥形的，父端明显粗于子端
- [ ] 关键路径的亮线在画布上可见，且与审计 tab 的 chip 链一致
- [ ] 期限四态弧长正确，逾期有呼吸动画
- [ ] `dormant` 是虚线空心，`done` 有对勾，`urgent` 有芽尖——灰度截图下三者仍可区分
- [ ] §7.0 的 11 项交互逐条实测通过
- [ ] 300+ 节点时降级规则生效，交互不卡顿

**待确认收件箱**
- [ ] 三类提案都能进入收件箱，聊天流有引用卡片可跳转
- [ ] 优先级信号滑块拖动时，分数变化预览实时更新且与实际应用后的结果一致
- [ ] 单条采纳/否决、批量操作、已处理区都正确
- [ ] 应用后树上有分数变化的动效反馈

**其他**
- [ ] 图例的 hover 教学联动生效
- [ ] 今日条无法被永久关闭
- [ ] 审计瀑布图的各段之和等于最终 `directPriority`
- [ ] 键盘可完成：切视图、切 tab、在树上移动选择、展开折叠、打开详情、发消息
- [ ] `prefers-reduced-motion` 下无循环动画
- [ ] 空状态的三个示例 chip 点击后正确填入输入框

**文档**
- [ ] `HANDOFF.md` 已更新，`MEMORY.md` 已追加设计决策
- [ ] 实现中所有偏离本文的地方，本文已同步修改
